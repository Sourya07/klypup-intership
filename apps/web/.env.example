# The base URL of the API server (must end with /api/v1 or /api)
# In production, change this to your deployed backend URL (e.g., https://your-backend.onrender.com/api/v1)
VITE_API_URL=http://localhost:8000/api/v1
